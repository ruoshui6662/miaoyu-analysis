# 妙舆开发计划 · 验收清单

> 工作方式：严格按顺序执行。当前阶段完成后，先按验收标准验证；只有验收通过，才进入下一阶段。
>
> 状态：⬜ 待开发 ｜ 🔄 开发中 ｜ ✅ 已开发待验收 ｜ ✅✅ 验收通过 ｜ ⛔ 验收未通过

## 最近交接记录（2026-09-05）

雷达已进入基础闭环实施：采用图一“左侧关键词栏+右侧单关键词时间线”，导航位于分析和历史之间；信息获取按官方 Feed、公开聚合、RSS Bridge、SearXNG、GDELT、Brave/Tavily 六层执行。当前代码先接入已启用的 feed/hotlist，完成主题管理、统一抓源、本地匹配和时间线；完整规范见 [`雷达功能规划与实施规范.md`](雷达功能规划与实施规范.md)。

雷达信源管理已完成专项设计定版，后续必须按 [`雷达信源管理设计规范.md`](雷达信源管理设计规范.md) 的 `RADAR-SRC-0~5` 顺序实施。第一阶段只允许建设 RSS/Atom、端点模型、主题绑定和真实同步状态；普通网站深抓、公共账号登录、Playwright 后台轮询及默认付费搜索均不属于当前阶段。

当前工程检查（2026-09-05）：编译、`git diff --check` 和前端脚本语法检查通过；全量测试共 76 项，全部通过；雷达接口冒烟和桌面浏览器新增主题交互验证通过。RSS/Atom 后端基线已开发，管理 UI、真实 Feed 人工验收、移动端人工验收与六层来源扩展仍待完成。

镜像发布链路：推送 `main` → `.github/workflows/docker-publish.yml` → GHCR `ghcr.io/ruoshui6662/miaoyu-analysis:latest`。目标 Docker/FNOS 更新时保留 `data` 数据卷，并重新拉取镜像后重建容器。

## 当前任务

| 顺序 | 阶段 | 目标 | 状态 | 验收结论 |
|---:|---|---|---|---|
| 1 | G0 工程基线收口 | 确认现有分析链路、热榜接口和降级逻辑稳定 | ✅✅ | 2026-09-02：当前 35 项全量测试通过；补充 AI 混合输出与风险对策链路验收 |
| 2 | G1 统一证据模型 | 将热榜和搜索结果统一保存为可追溯 Mention | ✅✅ | 2026-09-02：证据库、幂等入库、重启读取验收通过 |
| 3 | G1.5 热榜历史 | 保存榜单快照、排名变化和趋势 | ✅✅ | 2026-09-02：历史接口、排名变化、重复快照抑制验收通过 |
| 4 | G2 持续监测 | 支持关键词、定时采集、游标和失败重试 | ✅✅ | 2026-09-02：持久化、重启读取、失败游标保护验收通过 |
| 5 | G2.5 事件聚合 | 识别同一事件的跨平台传播和热度变化 | ✅✅ | 2026-09-02：跨平台归并、解释字段、累计热度和事件接口验收通过；真实业务阈值待上线前校准 |
| 6 | G2.6 告警与报告 | 支持突增、跨平台扩散和负面占比告警，并编排周期报告 | ✅✅ | 2026-09-02：告警规则、信号去重、周期报告接口和 JSON/Markdown/DOCX 产物验收通过；阈值待真实数据校准 |
| 7 | G3 安全与部署 | 完成鉴权、限流、日志、备份和上线检查 | ✅ | 本地与飞牛实例验收通过；Cloudflare/反向代理公网验收待完成 |
| 8 | G4 智能增强 | 聚类、事件演化、审核和多语言能力 | ⬜ | G3 通过后评估 |
| 9 | RADAR-0 雷达规划 | 图一 UI、六层信源、轻量时间线和零 AI 边界 | ✅ | 2026-09-05：规划与验收口径完成 |
| 10 | RADAR-1~4 雷达基础闭环 | 公开聚合源、本地多关键词匹配、API、UI | 🔄 | 2026-09-05：代码完成基础版；官方 Feed 增量、完整六层和移动人工验收待补 |
| 11 | RADAR-5 稳定性与六层来源 | 故障隔离、重启恢复、预算、24 小时抽查 | ⬜ | 基础闭环验收后继续 |
| 12 | RADAR-SRC-0 信源管理规范 | 来源/端点/绑定/状态分层，确定同步与安全边界 | ✅ | 2026-09-05：专项规范已写入，尚未修改正式代码 |
| 13 | RADAR-SRC-1 RSS 数据与同步基线 | 端点表、主题绑定、RSS/Atom Adapter、ETag/Last-Modified | ✅ | 2026-09-05：后端基线完成，76 项测试通过；真实 Feed 与重启现场验收待补 |
| 14 | RADAR-SRC-2 管理 API 与设置 UI | 检测、预览、CRUD、同步任务、健康状态和“雷达管理”页面 | ⬜ | 依赖 RADAR-SRC-1 达到 ✅✅ |
| 15 | RADAR-SRC-3 网站与公共账号适配 | Feed 自动发现、RSSHub/Bridge、按平台 Adapter | ⬜ | 依赖 RADAR-SRC-2 达到 ✅✅；禁止万能深抓和账号登录 |
| 16 | RADAR-SRC-4 稳定性与安全 | 数据库租约、退避、SSRF、凭据和 24 小时抽查 | ⬜ | 依赖 RADAR-SRC-3 达到 ✅✅ |
| 17 | RADAR-SRC-5 搜索补漏 | SearXNG/GDELT/条件付费 Provider | ⬜ | 前四阶段全部 ✅✅ 后另行确认预算和条款 |

## RADAR 专项验收入口

雷达不得直接把已有深度分析采集器包装成定时页面。实施和验收以 [`雷达功能规划与实施规范.md`](雷达功能规划与实施规范.md) 为唯一专项基线，关键硬门槛如下：

- [x] RADAR-0：图一 UI 与六层信源规划写入开发文档；
- [x] RADAR-1（基础版）：数据迁移、来源状态表、雷达订阅与游标字段；官方 Feed 增量、ETag/Last-Modified 待补；
- [x] RADAR-2（基础版）：每个已接入聚合信源每轮只抓一次，本地匹配所有关键词，排除词生效；
- [x] RADAR-3（基础版）：主题创建/列表/删除、手动刷新、启停、时间线、未读和来源健康 API；编辑与严格 cursor 分页待补；
- [x] RADAR-4（基础版）：导航位置、桌面左右布局、窄屏横向重排、空态/弹窗/刷新状态；完整移动端人工验收待补；
- [ ] RADAR-5：单源故障、付费预算、默认零 AI/零付费请求、24 小时稳定性抽查。

## RADAR-SRC 信源管理专项硬门槛

雷达信源管理以 [`雷达信源管理设计规范.md`](雷达信源管理设计规范.md) 为唯一专项基线。进入代码阶段前及每次合并前必须逐项确认：

- [x] RADAR-SRC-0：来源身份、采集端点、主题绑定和同步状态已拆分，接口、UI、安全及禁止事项已写入规范；
- [x] RADAR-SRC-1：只实现 RSS/Atom 基线；同一端点每轮只请求一次，ETag/Last-Modified、304、游标和去重均有离线测试；真实 Feed 与重启现场验收待补；
- [ ] RADAR-SRC-2：检测成功后才允许保存；同步返回任务状态；页面明确区分“无匹配/无来源/采集失败”；
- [ ] RADAR-SRC-3：网站先发现 Feed，没有 Feed 时不得静默使用通用深抓；公共账号仅允许官方 Feed/API 或管理员配置的 Bridge；
- [ ] RADAR-SRC-4：多进程使用数据库租约；SSRF、内网白名单、响应大小、超时、退避和凭据不回传全部通过；
- [ ] RADAR-SRC-5：默认 AI 与付费调用为 0；只有前序阶段全部 ✅✅ 且预算/条款单独确认后才能开发。

以下行为直接判定验收失败：使用 Playwright 作为默认后台抓取器；保存账号密码或个人 Cookie；按关键词重复抓取同一来源；删除来源时清空历史 Mention；未授权访问任意内网地址；后台静默调用 Brave/Tavily。

## G0 当前验收单

### 已完成的自动验收

- [x] `python -m unittest discover -s tests -v`：当前 26 项全量测试通过
- [x] `python -m compileall -q backend tests`：通过
- [x] Flask `/healthz`：HTTP 200，返回 `ok: true`
- [x] 热榜测试覆盖 NewsNow 标准化、本地缓存、六榜归一和付费源默认关闭
- [x] 失败隔离、配置热加载、URL 缓存隔离、安全文件名测试通过

### G0 补充验收结果

- [x] 信源设置页关闭一个来源并保存，再恢复并保存；页面显示“已保存 4 项设置，立即生效”
- [x] 使用 AI 测试替身走通 `/api/analyze → 后台任务 → /api/tasks/<id>`，任务完成状态和报告摘要正确
- [ ] 设置页修改 SearXNG 地址或采集组后，不重启服务发起真实分析，确认新设置生效（真实服务抽查）
- [ ] 使用真实 AI 分析，确认进度显示真实阶段，不出现虚假的“已生成 N 字”（真实服务抽查）
- [x] 离线使用包含 `/ : * ? < > |` 的主题生成 Markdown/DOCX，两个文件均正常
- [x] 打开首页，六个热榜均正常显示，每榜 15 条；页面无控制台错误
- [x] 热榜卡片显示 NewsNow 来源状态；当前页面数据正常加载
- [x] 故障注入单测确认单个公开榜失败不会丢失其他榜单
- [x] 离线生成含特殊字符主题的 Markdown/DOCX，两个文件均成功生成且 DOCX 非空
- [x] `<think> + JSON`、代码围栏和前后解释文本均可解析；无 JSON 时只重试一次并明确报错
- [x] 风险漏填 `id` 时自动补齐 `r1/r2...`，对策仍可按风险逐条匹配
- [x] 单榜失败隔离已通过自动故障注入测试；浏览器运行时故障注入作为部署后抽查

### G0 验收结果填写

```text
验收人：
验收时间：
结果：通过 / 未通过
问题：
```

当前记录：26 项自动化/集成测试、编译、健康检查、首页加载、信源保存、单榜故障隔离、任务链路、离线导出和 AI 输出容错已通过，G0 关闭；真实 AI 与部署环境抽查不阻塞后续阶段。

## 后续阶段的进入规则

1. 当前阶段所有必选验收项通过后，状态改为 `✅✅`。
2. 验收不通过，记录问题并回到当前阶段修复，不进入下一阶段。
3. 验收通过后，只启动下一阶段的开发，不提前实现更后面的功能。
4. 每个阶段完成后，同步更新本文件、`docs/开发手册.md` 和测试记录。

## G1 预览：统一证据模型

G0 通过后，下一步只做数据模型和入库，不做定时任务、不做复杂 AI 聚类。

- [x] 创建 `scan_runs`
- [x] 创建 `mentions`
- [x] 创建 `hot_items`
- [x] 创建 `cursors`
- [x] 定义统一 SourceAdapter 接口
- [x] 将当前热榜条目转换为 Mention 并幂等入库
- [x] 验收：应用重启后数据仍在，同一内容重复采集不重复入库

## G2 当前验收单

- [x] 创建 `topics`、`subscriptions`、`monitor_runs` 表
- [x] 主题关键词支持 JSON 保存和恢复
- [x] 调度器单进程启动，任务不重入
- [x] 采集成功后才提交 cursor；失败不推进 cursor
- [x] 失败重试使用退避，连续失败进入冷却
- [x] 验收：创建主题后执行一次监测，重启服务后订阅和游标仍在（API 200；重启后主题与订阅仍可读）
- [x] 验收：模拟来源失败，任务状态为失败且 cursor 不变（单元/集成测试通过，失败退避生效）

## G2.5 当前验收单

- [x] 事件表与事件-Mention 关系表持久化，按主题隔离
- [x] 标题规范化后优先精确匹配，近似标题使用可解释的 `title_overlap`
- [x] 跨平台 Mention 可归并到同一事件，保留匹配方法
- [x] 热度按 Mention 数、平台数、热度值和新鲜度计算，并提供事件接口
- [x] 自动测试：跨平台标题变体归并、平台数/提及数、热度大于 0
- [x] 自动测试：`GET /api/monitor/events` 主题校验与空结果
- [x] 工程验收：用固定跨平台标题变体确认事件归并、跨平台扩散和热度排序
- [ ] 业务验收：用真实监测结果确认事件归并、跨平台扩散和热度排序符合业务判断（上线前校准）

## G2.6 当前验收单

- [x] `signals` 表持久化，按主题/事件/信号类型去重
- [x] 支持跨平台传播、连续新增提及、负面占比三类确定性规则
- [x] 负面占比只使用已明确标注的情感，不把未标注内容误判为负面
- [x] 监测成功后自动评估信号，失败任务不生成成功信号
- [x] 自动测试：三类信号生成且重复评估不重复入库
- [x] 自动测试：`GET /api/monitor/signals` 主题校验与空结果
- [x] 周期报告：将监测窗口、事件、信号和证据编排为可下载 JSON/Markdown/DOCX 报告
- [x] 现场验收：报告接口返回 201，三种文件均实际生成
- [ ] 业务验收：用真实数据校准突增阈值、负面比例阈值和通知策略

## G3 当前验收单

- [x] 管理员访问：网页固定使用 `admin` 账号；首次密码为 `password`，密码以 PBKDF2 加盐哈希持久化到 `data/admin_account.json`，支持设置页修改密码；旧版 Bearer `MIAOYU_ADMIN_TOKEN` 保持兼容
- [x] 私有 API 未带令牌返回 401；令牌请求支持 Bearer Header 和 HttpOnly 会话 Cookie
- [x] 401 不返回 `WWW-Authenticate` 浏览器挑战；前端不使用 `prompt()`，未登录显示站内登录表单
- [x] 站内登录：校验至少 6 个字符的用户自定义令牌，使用 HttpOnly 会话 Cookie，支持退出登录；短令牌明确拒绝
- [x] A3 飞牛现场验收：用户反馈飞牛容器中使用 `MIAOYU_ADMIN_TOKEN=password` 登录成功，登录遮罩正常关闭
- [x] `/api/settings` 不返回 API Key；脱敏保存不会清空已有 Key
- [x] 请求体上限、固定窗口限流、受限 CORS 和安全响应头
- [x] JSON 结构化 HTTP/监测日志，包含 request_id 和 scan_run_id
- [x] 受保护 `/api/metrics` 只返回运行计数，不返回密钥
- [x] 备份/恢复工具覆盖 SQLite、任务素材和报告，并拒绝不安全路径；恢复必须显式确认
- [x] 本地生产模式验收：首次请求生成 43 字符令牌；无令牌 401；Bearer 后 HttpOnly Cookie 可复用；设置响应无 API Key；安全头/默认拒绝跨域生效
- [x] 本地备份验收：实际生成 `data/backup-g3-acceptance.zip`；恢复覆盖保护和 SQLite/报告恢复由自动测试验证
- [x] 本地运行时验收（2026-09-03）：首页可打开且无浏览器错误；`/healthz` 返回 200；`/api/auth/status` 返回未认证；未登录访问 `/api/settings` 返回 401；站内登录表单正常显示
- [x] 部署预检：`python backend/preflight.py` 通过；`--require-docker` 在无 Docker 时明确失败，不掩盖现场缺失
- [x] 飞牛实例现场复验（2026-09-03）：`/healthz` 返回 200；未登录 `/api/settings` 返回 401；`/api/hot/boards` 返回 200，固定六榜且每榜 15 条；用户已确认飞牛容器密码登录成功
- [ ] Cloudflare/反向代理公网验收：公网主机名、Tunnel 转发、源站端口隔离和 HTTPS 登录链路

## A1b 当前验收单

- [x] 可选覆盖文件：默认 Compose 行为不变，使用 `docker-compose.searxng.yml` 才启动内置搜索栈
- [x] 服务契约：`yuqing` 通过 Compose 内网访问 `http://searxng:8080`，SearXNG 不映射宿主端口
- [x] 初始化契约：首次创建命名卷时生成随机 `server.secret_key`，已有 `settings.yml` 不覆盖
- [x] 持久化契约：SearXNG 配置、缓存和 Valkey 数据分别使用命名卷
- [x] 静态验收：YAML 可解析，四个服务、依赖条件、内网 URL 和初始化命令检查通过
- [x] 回归验收：全量 38 项测试、Python 编译和代码格式检查通过
- [ ] 目标环境验收：Docker Compose 启动后 `yuqing`、SearXNG、Valkey 均健康，并完成一次真实 JSON 搜索

## A2 当前验收单

- [x] FPK 源目录：`fpk/miaoyu/manifest`、`config/resource`、`config/privilege`、`cmd/main` 和 Docker Compose 目录齐全。
- [x] Manifest：应用名 `miaoyu`、版本 `0.1.0`、平台 `all`、服务端口 `5000`、桌面入口定义完整。
- [x] Docker 资源：Compose 使用 `${TRIM_SERVICE_PORT}`、`${TRIM_PKGVAR}` 和 `${TRIM_PKGETC}`，数据与配置路径符合 fnOS 运行目录约定。
- [x] 生命周期：首次安装生成 `.env` 模板，升级不覆盖现有配置；`cmd/main status` 按 `miaoyu` 容器状态返回 0/3。
- [x] 安全边界：FPK 源包不包含 `.env`、运行数据、Node 依赖和真实密钥；镜像通过 GHCR 拉取。
- [x] 本地验收：`python scripts/build_fpk.py --dry-run` 通过；FPK JSON/YAML 契约通过；`python -m unittest discover -s tests -v` 当前 42 项通过；`compileall` 与 `git diff --check` 通过。
- [ ] 使用飞牛官方 `fnpack build` 生成真实 `.fpk` 文件。
- [ ] 目标 fnOS 安装验收：安装、启动、桌面入口、`/healthz`、热榜、登录、报告生成、重启数据保留和升级配置保留。
- [ ] 目标设备架构验收：确认 GHCR 镜像与实际 x86/ARM 设备兼容。
