# Design QA — 搜索服务 / 分析模型统一工作台

## 核对范围

- 选定设计参考：`C:\Users\admin\.codex\generated_images\01a061d7-fe97-7643-8a74-52cbd4b16427\exec-1b6d3cb1-de46-4181-ba7b-3a97d15852bc.png`
- 参考图尺寸：1487 × 1058
- 本地实现：`http://127.0.0.1:5000/?search-instances=check`
- 搜索服务截图：`D:\AI编程\舆情\tmp\design-qa-1.png`
- 分析模型截图：`D:\AI编程\舆情\tmp\design-qa-2.png`
- 实现截图尺寸：1402 × 1272；浏览器默认缩放，按对应页面内容区域进行比较。

## 第一性原理约束

1. 一级设置导航是产品信息架构，不属于本次重构范围，骨架、图标、间距与选中态保持不变。
2. 两个页面的共同任务都是“选择一个服务实例并编辑参数”，因此共用“左侧实例索引 + 右侧参数编辑器”的主从结构。
3. 视觉层级优先依靠留白、字号、弱底色和状态色，不新增大卡片、重阴影或大圆角。
4. 不改变后端接口、配置数据结构和保存流程，降低 UI 改造对既有功能的风险。

## 全局对比

- 页面标题、说明文字、内容起点与一级导航均保持原有产品骨架。
- 搜索服务和分析模型统一为 208px 内层索引栏与弹性编辑区，桌面端层级和阅读顺序一致。
- 统一了实例行高度、激活态红色竖线、淡红选中底、图标尺寸、字段高度和按钮尺寸。
- 继续使用冷白背景、低对比边框和小圆角，未引入额外大卡片。

## 局部对比

### 搜索服务

- 原弹窗编辑改为页面内编辑，减少视线跳转和上下文丢失。
- 已有实例编辑时隐藏不可变的服务商选择，仅保留实例名称和该服务商实际需要的参数。
- 新增实例时显示服务商选择，并根据 SearXNG、Keenable、Brave Search、Tavily 动态渲染字段。
- 调度策略保留在编辑器下方，调用顺序仍由实例索引栏控制。

### 分析模型

- 沿用现有服务商选择与参数编辑逻辑，仅统一密度、字段、按钮、状态与选中样式。
- 内置服务商的“删除服务商”操作正确隐藏，自定义服务商才展示删除能力。

## 交互核对

- 设置页一级导航可切换“搜索服务”和“分析模型”。
- 搜索实例可选择、进入新增状态、切换内置服务商并取消新增。
- 已保存实例刷新后仍被选中并回填参数。
- 分析模型可切换服务商，现有测试连接、获取模型、保存并启用入口保持可用。
- 本轮未主动调用第三方测试连接，避免产生外部请求或额度消耗。

## 迭代记录

- 第 1 轮发现内层索引栏偏窄、实例名易截断，宽度由 192px 调整为 208px。
- 第 1 轮发现已有搜索实例仍展示不可编辑的服务商下拉，改为编辑态隐藏并让实例名称占满一行。
- 第 1 轮发现按钮组件样式会覆盖原生 `hidden` 行为，补充作用域规则，修复内置模型误显示删除按钮。
- 第 2 轮浏览器复核未发现 P0、P1 或 P2 视觉与交互问题。

## 剩余低优先级差异

- P3：参考图是双页面展示板，实际产品一次只展示一个设置页面；保留真实路由与导航行为。
- P3：参考图中的部分服务商图标为概念图形，实现继续使用产品现有字母标识，避免额外资产变更。

## 验证

- 浏览器：Codex in-app browser，桌面视口 1402 × 1272。
- 自动化：`python -m unittest tests.test_g0_baseline`，70 项通过。
- 代码检查：`git diff --check` 通过。

## Final result

passed

## 按钮统一核对（2026-09-05）

- 设置页操作按钮收敛为统一组件契约：40px 高、13px 字号、600 字重、6px 小圆角、8px 内部图标间距。
- 普通操作按钮统一最小宽度 80px；保存、启用等主要操作统一最小宽度 104px；“获取模型”“检测 Feed”按文字长度保留 84px / 92px 的最小宽度。
- 规则已覆盖搜索服务、分析模型、信息来源、雷达管理、账户管理和设置页底部操作区；移动端保持 40px 高，不再被旧的 44px / 52px 规则放大。
- 浏览器复核：搜索服务、分析模型页面的测试、删除、保存、获取模型按钮高度与字重已对齐，主次层级仅由颜色和语义区分。
