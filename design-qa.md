# 妙舆 UI 设计验收

## 验收范围

- 目标视觉：设置页效果图（图一的浅色编辑台式工作台）
- 来源视觉真值：[codex-clipboard-f8f0dee9-4252-445b-a308-541334193486.png](C:\Users\admin\AppData\Local\Temp\codex-clipboard-f8f0dee9-4252-445b-a308-541334193486.png)
- 本地实现：http://127.0.0.1:5001/
- 页面状态：设置 / 基本设置；另行切换验证 AI 接口、信源采集、分析、历史

## 截图与尺寸

- 来源截图：1232×879 px；桌面设置页效果图。
- 实现截图：Codex In-app Browser tab 1 已捕获，当前桥接层不暴露可引用的本地文件路径；截图 509×687 px，CSS 视口 517×698，设置为移动响应状态。
- 密度：实现截图按浏览器桥接输出记录，未进行像素级缩放比较；桌面宽屏以 CSS 断点与目标几何关系做静态复核。
- 核心比较区域：设置外壳、左侧功能区、右侧标题/表单/保存动作；未比较浏览器外壳和不可见的全局导航裁切区域。

## 对比结论

- 全视图：来源是桌面左栏布局，实现页在当前窄视口正确折叠为横向分段；桌面 CSS 保持来源的白色大工作台、左侧低对比度功能区和右侧单一内容流。
- 聚焦区域：桌面规则为 196px 网格轨道 + 20px 间隔 + 右侧 20px 起始内边距，形成目标图中的“左栏结束 → 内容开始”节奏；左栏 44px 行高、暖红激活态、底部说明与来源一致。移动实测无横向溢出。

## 设计表面检查

- 字体与排版：沿用系统字体栈；页面标题、面板标题、说明文字和表单标签保持三级层级，面板标题微调至 24px，避免右侧内容显得过轻。
- 间距与布局：设置工作台保留 520px 桌面最小高度；左栏上下内边距、右侧内容 32/40/20px 内边距、字段与保存区间距按效果图校准。
- 颜色与令牌：沿用暖红主动作、暖白背景、浅灰表面和低对比度分隔；未新增页面级硬编码颜色。
- 图标与资产：设置菜单图标使用现有统一线性 SVG，补充 `aria-hidden="true"`，不引入 emoji 或占位图形。
- 文案与内容：保留“基本设置 / AI 接口 / 信源采集”职责；保存即生效说明在左栏和动作区各出现一次，分别服务定位与操作确认。

## Findings

### P0 — 阻塞问题

- 未发现。

### P1 — 主要问题

- 未发现。设置页仍保持单一主内容区，左侧只负责功能定位；既有配置、服务商与信源交互未改变。

### P2 — 中等问题

- 桌面宽屏截图未能在当前 In-app Browser 视口中导出：当前 CSS 视口为 517×698，无法与 1232×879 来源图做同尺寸像素级对照。桌面实现已依据来源几何关系静态复核，需用户在宽屏浏览器做最后视觉确认。

## 本轮迭代记录

1. 初版问题：设置页虽然已具备左侧结构，但左栏留白、面板标题比例、保存动作尺寸与效果图仍有细微偏差。
2. 已执行修复：左栏内边距从 20px 调整为 22px；标题调整为 24px；保存按钮固定为 128px 最小宽度、56px 高度、16px 字号；移动端隐藏设置横向滚动条；菜单图标补充装饰性无障碍标记。
3. 修复后证据：浏览器实测基本设置页，左侧功能区在移动端横向分段显示；保存按钮全宽、输入框无裁切；AI 接口与信源采集子页切换正常；浏览器控制台无新增错误。

## 行为与回归

- 设置页基本设置、AI 接口、信源采集切换通过。
- 分析页、历史页导航与现有核心输入/列表保持可用。
- 浏览器控制台错误：0。
- 移动端文档流横向溢出：0（`document.documentElement.scrollWidth - innerWidth`）。
- Python 回归测试：45/45 通过（`python -m unittest discover -s tests -p 'test_*.py'`）。
- 前端内联脚本解析通过；`git diff --check` 通过。
- 本轮未再次执行 GitHub 推送；工作区仅完成首页实现与文档更新。

## 最新执行：首页图一落地（2026-09-03）

- 目标视觉：[codex-clipboard-1c43ca7f-9fb4-4901-92a5-b11681ef20c9.png](C:\Users\admin\AppData\Local\Temp\codex-clipboard-1c43ca7f-9fb4-4901-92a5-b11681ef20c9.png)
- 实现区域：`#view-home`，居中研判入口、当前关注焦点、刚刚发生、当前热榜四段式信息路径。
- 数据证据：浏览器 DOM 实测 `90` 条热榜行、`7` 个页签、4 条刚刚发生；平台页签切换到微博后为 15 条。
- 响应式证据：当前 CSS 视口 `857×822`，页面 `scrollWidth=849`，无横向溢出；热榜容器 `scrollHeight=5821`、`clientHeight=440`，纵向滚动条隐藏。
- 运行时证据：浏览器开发日志为空；内联 JavaScript 解析通过。
- 当前限制：In-app Browser 的截图桥接仍返回旧视觉缓存，且当前视口不是来源图的宽屏尺寸，因此未宣称像素级桌面验收通过。用户可在宽屏浏览器直接刷新 `http://127.0.0.1:5001/` 做最终视觉确认。

## 最新执行：分析 / 历史 / 设置页面家族（2026-09-03）

- 视觉标准：沿用图一首页和已确认设置页效果图的暖白背景、顶部导航、页面眉标、白色工作台、暖红主动作与低对比度分隔。
- 分析页实测：页面头 `809×100`、研判入口 `809×120`、最近分析 `809×362`；主输入与参数分层，原有主题、服务商、事实校验和开始研判 ID 保持不变。
- 历史页实测：历史任务和报告归档均为 `809px` 宽连续工作台，当前页渲染 10 条任务；任务状态、主题、时间与详情保持可扫读层级。
- 设置页实测：工作台 `809×540`，左侧功能区 `180×508`，右侧内容区 `593×540`；基本设置显示全局保存，AI 接口和信源采集切换后该按钮隐藏，各自使用面板内动作。
- 交互证据：设置三个面板切换通过；AI 服务商编辑区、10 个信源分组正常渲染；三个页面横向溢出均为 0。
- 当前限制：浏览器截图桥接仍返回首页旧缓存画面，无法对三个页面生成可信的同视口实现截图，因此本轮视觉 QA 继续标记为 blocked，等待用户在实际宽屏浏览器确认。

### 历史任务卡边距回归

- 根因：旧选择器 `#view-history > .card:nth-child(2)` 的优先级高于新组件规则，把历史任务卡内边距覆盖为 0。
- 修复：删除过期覆盖，并使用明确的直属组件选择器固定桌面 `24px 28px 20px`、移动端 `20px 18px` 内边距。
- 浏览器验证：标题区和任务列表左右内缩 28px；总数与返回按钮距右边缘 30px；10 条任务行 `scrollWidth` 与 `clientWidth` 一致，页面横向溢出为 0。

## Implementation Checklist

- [x] 图一全局顶栏与页面家族规则保持不变
- [x] 设置桌面端左侧功能区、右侧内容区
- [x] 设置移动端横向分段与滚动条隐藏
- [x] 基本设置、AI 接口、信源采集交互保持可用
- [x] 预留现有 API / DOM 槽位，不新增业务协议
- [x] 浏览器关键状态、控制台和回归测试已检查
- [ ] 用户在宽屏视口完成最终视觉确认

final result: blocked
