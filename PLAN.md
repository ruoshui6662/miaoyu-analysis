# 舆情分析系统 · 规划文档（v0.1）

> 目标：Web 页面 + SearXNG 信息抓取 + AI 接口分析整理，报告严格对齐 `参考格式.docx` 模板。
> 本文件：可行性、工作原理、信源方案、问题清单、分期规划。评审通过后进入实施。

> **文档状态（2026-09-02）**：本文件保留为立项期的可行性与原始 P0-P3 依据，不能再用其中的未勾选项判断当前进度。当前执行计划以 `docs/开发手册.md` §3/§8 和 `docs/开源项目调研与路线图.md` 为准：已从“单次主题→报告”的闭环，转向 **G0 工程收口 → G1 统一证据模型 → G2 增量监测/告警 → G3 安全交付 → G4 分析增强**。

> 原规划映射：P0-P2 的最小闭环与页面化已完成主体实现；P3 不再笼统推进，拆分为 G1（采集/证据）、G2（监测）、G3（安全交付）、G4（质量与模板）。受限社媒深爬、重型多 Agent、队列/微服务均不作为默认路线，需以合规与实测瓶颈另行立项。公开 API 前置与多源轮询按 `docs/API接入前置与多源轮询方案.md` 执行。

---

## 0. 结论先行

**技术上完全可行，本方案最大风险不在工程，而在"数据可得性"。**
SearXNG 只能提供"搜索结果"（标题 + 摘要 + 链接），提供不了正文全文，更抓不动微博/小红书/抖音的深水区内容。
因此必须设计 **三层信源**（搜索层 → 正文层 → 热点层），并明确"能自动到什么程度、需要人工补什么"。

**环境实测结论（2026-08-31 本机）：**

| 项 | 结果 | 影响 |
|---|---|---|
| Python 3.12.10 | 可用 | 后端主栈 |
| Node 24 / npm 11 | 可用 | 报告生成走 Node `docx` 库 |
| Docker | **未安装** | SearXNG 用 pip 安装或后期补 Docker |
| lxml | **被企业"应用程序控制策略"拦截**（python-docx 无法导入） | 不能用 python-docx 生成 Word，改用 Node `docx`（纯 JS 无原生依赖） |
| pandoc | 未安装 | 不依赖 |
| 网络 | DeepSeek API / Bing / Google 均可达 | AI 与搜索无网络障碍 |

---

## 1. 第一性原理：真实需求拆解

表面需求：一个"页面 + SearXNG + AI 接口"。
本质需求：**输入一个主题 → 输出一份符合 `参考格式.docx` 模板的舆情分析报告**。

从模板反推（已解析 docx 结构）：

```
标题（a3 样式，28pt 居中）
引言段                    ← 事件背景 + 定性，概括全文
一、事件概况梳理          ← 时间线叙事：谁、何时、何地、说了什么
二、原因分析              ← 多因素归因（社会/经济/心理…），每个因素一个分论点段落
三、风险分析              ← 多维风险（法律/商业/社会…）
四、对策建议              ← 分方向的举措（句首短词 + 论述）
```

关键认知：这是**政务/研究型四段式**结构，与 BettaFish 的
"8 章商业报告结构"（执行摘要/声量/事件回顾/情感/画像/风险机遇/SWOT…）**不同**。
→ 可以借鉴 BettaFish 的**多角色 Agent 流程与反思机制**，但**报告生成器必须按用户模板定制**，不能直接复用其输出结构。

---

## 2. 可行性分析

### 2.1 技术可行性 —— 可行
| 环节 | 方案 | 备注 |
|---|---|---|
| 聚合搜索 | SearXNG（pip 或 Docker），JSON API `?format=json` | 需自建实例并开 json 格式 |
| 正文抓取 | 后端 `requests` + 解析（纯 stdlib 即可）；动态页用 Playwright（后期） | 不依赖被拦截的 lxml |
| AI 分析 | OpenAI 兼容协议：DeepSeek / 通义 / GLM / OpenAI 均可 | DeepSeek 实测可达 |
| 报告生成 | Node `docx` 库（Word）+ 自写 HTML 渲染（预览） | 绕开 lxml 限制 |
| 前端 | FastAPI + 单页 HTML/JS（或 Vue 用 CDN） | 本地 Web 应用 |

### 2.2 数据可行性 —— 分层困难
| 数据 | 可得性 | 手段 |
|---|---|---|
| 新闻/门户/网页 | 高 | SearXNG 搜索 + WebFetch 正文 |
| 知乎/B站/贴吧 | 中 | `site:` 搜索 + 热榜 API |
| 微博 | 中（搜索有限） | 热搜榜 + site 搜索 |
| 微信公号 | 低-中 | 搜狗微信搜索 / 合规 API |
| 小红书/抖音 | 低 | 反爬强；降级为 site 搜索 + 标题级信息；或接付费舆情 API |

**结论：能自动覆盖"全网可搜索 + 热榜热点"层面；社媒深水区要么降级要么付费。**

### 2.3 成本可行性 —— 可控
单次分析 AI token 消耗大（原始素材 + 分章生成），需：
- 分段喂给 AI（按事实/归因/风险/对策分角色），不一次性塞全文
- 素材先摘要压缩再入报告生成
- 报告生成单章输出（避免超长截断）
- 结果与素材落盘缓存，重复分析不重花

### 2.4 合规可行性 —— 必须遵守
- 抓取遵守平台条款与 robots；控制频率，不绕登录墙强行爬取
- 个人信息脱敏（模板已体现"用户化名"需求）
- 报告中每条论断标注来源（媒体 + URL + 日期），这是防 AI 幻觉也是合规底线
- 涉密/敏感内容不采集不扩散；报告使用责任归使用者

---

## 3. 工作原理（系统架构）

```
[前端页面] 输入主题 → 任务进度 → 报告预览/下载（docx / html）
      │
[后端 FastAPI]
  ├─ 任务编排器：主题 → 关键词扩展(AI) → 搜索计划（多词×多引擎×时间窗）
  ├─ 采集层
  │    L1 SearXNG JSON（聚合搜索：bing/google/baidu/news/wiki…）
  │    L2 正文抓取（新闻/网页全文）+ 失败降级
  │    L3 热榜 API（微博热搜/知乎热榜/B站热门/百度热搜）→ 事件发现与时效补缺
  ├─ 清洗层：字段标准化 / 标题-SimHash 去重 / 来源平台分类 / 三级可信度标记
  ├─ 分析层（AI 多角色流水线，借鉴 BettaFish 三引擎思想）
  │    ① 事实整理（事件脉络、时间线、关键主体）
  │    ② 归因分析（多因素，对应模板"原因分析"）
  │    ③ 风险识别（多维，对应模板"风险分析"）
  │    ④ 对策生成（分方向，对应模板"对策建议"）
  │    ⑤ 校验轮：每条论断必须挂来源；无来源事实删除/降级；自检一次 → 重写
  ├─ 报告层：模板渲染（对齐参考格式.docx）→ Node docx 生成 + HTML 预览
  └─ 存储：SQLite/JSON（任务、素材、中间态、报告）
```

**与 BettaFish-skill 的融合点（只借骨架，不借输出格式）：**
1. 多 Agent 并行/流水线分析 → 简化为"搜索规划 / 事实整理 / 归因风险 / 报告撰写"四角色
2. "3 轮反思循环" → 简化为"报告生成前校验轮 + 缺源重写"
3. 6 种模板场景（品牌声誉/危机事件/社会热点/竞品/政策/定期监测）→ 保留场景识别，但**统一输出为用户的四段式模板**
4. 三级可信度（官方/权威 = 高，KOL/用户 = 中，匿名/单源 = 低）→ 直接采用

---

## 4. 信源方案（需要接入的信源清单）

| 优先级 | 信源 | 形态 | 用途 | 接入方式 |
|---|---|---|---|---|
| ★★★ | SearXNG 自建实例 | 聚合搜索 | 主干采集 | pip 安装，配 bing/google/baidu/news/wiki 引擎，开 json |
| ★★★ | 新闻正文抓取 | WebFetch | AI 分析素材 | requests + 正文抽取 |
| ★★☆ | 微博热搜 / 知乎热榜 / B站热门 / 百度热搜 | 公开 API/页面 | 事件发现、时效补缺 | 轻量抓取，做成统一热榜接口 |
| ★★☆ | 微信公号 | 搜狗微信搜索 | 舆论深水区（重要） | 搜索接口 + 正文抓取（反爬中） |
| ★☆☆ | 小红书 / 抖音 | site: 搜索 | 标题级信息 | 降级策略：只取标题/摘要/互动数据 |
| ★☆☆ | 付费舆情 API（清博/微热点/识微等） | API | 权威完整数据（可选） | 有预算时接 |
| ★☆☆ | RSS 订阅源 | RSS | 行业媒体轮询 | feedparser/自写 |
| 支撑 | 官方声明/权威媒体原文 | WebFetch | 三级可信度中的"高可信锚点" | 搜索时打标 |

**最优性价比：先接"热榜 API"，一天工作量，补掉搜索引擎时效性最大的缺口。**

---

## 5. 问题与不足清单（按严重度）

1. **【高】SearXNG 只有搜索结果，没有正文** —— 只喂标题摘要生成的报告必然空洞。
   对策：正文抓取层是 P1 的硬性组成部分，不是可选优化。
2. **【高】社媒深水区覆盖弱**（小红书/抖音/微信），且改版频繁。
   对策：明确产品边界 = "全网可搜索信息 + 热榜热点"，深水区降级或付费，不假装能覆盖。
3. **【中高】时效性缺口**：热点爆发 0–6 小时搜索引擎索引不到。
   对策：热榜 API 最先接入。
4. **【中高】AI 幻觉**：报告可能编造事件、引文、来源。
   对策：硬约束"论断挂来源" + 校验轮 + 生成后抽样核对 URL 可达。
5. **【中】噪声污染**：营销号、转载稿、聚合站稀释信号。
   对策：SimHash 去重、来源权重、可信度三级标记展示。
6. **【中】模板对齐风险**：机器生成的"四段式"容易写成空话套话。
   对策：模板结构 + 每条断言要求数据/来源支撑；沿用示例报告的"分论点段落 + 句首短词"行文。
7. **【中】本机环境限制**：lxml 被企业策略拦截（python-docx 不可用）、无 Docker。
   对策：报告生成用 Node `docx`；SearXNG 先 pip 装。
8. **【中】成本**：长文本 token 消耗。
   对策：分段分析 + 素材压缩 + 缓存。
9. **【低】合规**：脱敏、频率控制、敏感信息边界。
   对策：工程内联规则 + 报告免责声明页。
10. **【低】长任务体验**：一次分析几分钟，页面需异步任务/进度/重试。

---

## 6. 分期规划

### P0 基础设施（0.5–1 天）
- [ ] SearXNG pip 安装并本地运行，配置可用引擎 + 开启 json API，验证搜索可用
- [ ] 确定 AI 接口与 key（待拍板），写"素材 → 结构化 JSON"的最小 prompt 验证
- [ ] 用 Node `docx` 按模板规格生成一个样例 docx（验证 a3 标题 28pt / 章节 / 正文行文）
- [ ] 建目录与最小框架（backend / frontend / data）

### P1 最小闭环（1–2 天）—— 先跑通"主题 → 四段式报告 docx"
- [ ] 关键词扩展 → SearXNG 批量查询（多词 × 多引擎 × 时间窗）
- [ ] 正文抓取 + 清洗去重 + 可信度标记
- [ ] AI 四角色流水线：事实整理 → 归因 → 风险 → 对策 → 校验轮
- [ ] 报告渲染：对齐模板生成 docx（+ 便于调试的 markdown）
- [ ] 用一个真实主题端到端验证，人工校对缺失项

### API-0/API-1 公开 API 前置（1–2 天）
- [x] 中国热榜优先：接入 NewsNow JSON（微博/知乎/B站/抖音/百度/头条），并保留 TopHub/REBANG HTML 降级
- [x] 首页返回 provider、榜单 source_id、rank、来源健康状态；NewsNow 5 分钟本地缓存，单榜失败隔离
- [x] 付费接口默认关闭（`PAID_APIS_ENABLED=false`），TopHubData 仅保留后续切换位
- [ ] 统一 `Mention` 字段、来源适配器契约、URL/外部 ID 去重键（API-2 前完成）
- [ ] 接入 GDELT DOC/Context、RSS/Atom、Hacker News；为每个来源记录抓取时间与健康状态
- [ ] 确认本机 SearXNG 部署形态，并完成 JSON API、超时、缓存和降级验证
- [ ] 不把微博/知乎/抖音/小红书登录态或非官方接口作为基础前置

### P2 Web 页面化（2–3 天）
- [ ] FastAPI 后端：任务接口、进度、历史
- [ ] 前端页面：主题输入、采集进度、报告预览（docx 下载 + HTML 可视化）
- [ ] 素材库可视化（可选：来源列表、去重统计）

### P3 增强（持续，按需）
- [ ] 热榜信源接入（微博/知乎/B站/百度）
- [ ] 微信搜狗渠道 + 社媒降级策略
- [ ] 定时监测任务（每日/每周自动出报告）
- [ ] 多场景映射（品牌/危机/政策/竞品，输出仍是四段式模板）
- [ ] 成本与质量监控（token 用量、幻觉抽查）

---

## 7. 实施前需拍板的选型

1. **AI 接口**：建议 DeepSeek（OpenAI 兼容、中文强、实测可达；通义/GLM 亦可）
2. **SearXNG 形态**：现状无 Docker → 先 pip 本地安装；或先用公共实例联调
3. **页面部署**：本地单机 Web（localhost）还是局域网服务
4. **首期范围**：先做"指定主题一键分析"，还是先做"热榜热点发现 + 点击分析"
